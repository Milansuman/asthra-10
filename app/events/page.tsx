import { PurchaseCardCard } from '@/components/ui/purchase-preview-card';
import React from 'react';
// import { eventZod } from '@/lib/validator';
// import { z } from 'zod';

// type Event = z.infer<typeof eventZod>;

const EventsPage = () => {
  return (
    <div className="flex w-screen relative bg-gray-500">
      {/* <PurchaseCardCard
        title="Asthra Pass"
        description="Get access to all events and explore the world of Asthra"
        features={[
          'Access to campus',
          '400 tokens for events',
          'Just 200 per head',
        ]}
      /> */}
    </div>
  );
};

export default EventsPage;
